# Copiá este archivo a "keystore.properties" (sin el .example) en esta misma
# carpeta, y completá los valores reales. NUNCA se commitea (ver
# android/.gitignore) — vive solo en tu compu.
storeFile=../fineticap-release.jks
storePassword=CAMBIAR
keyAlias=fineticap
keyPassword=CAMBIAR
